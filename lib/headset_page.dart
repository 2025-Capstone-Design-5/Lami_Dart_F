import 'package:flutter/material.dart';

class HeadsetPage extends StatelessWidget {
  const HeadsetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('헤드셋 페이지'));
  }
}